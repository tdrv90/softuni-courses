function weddingSeats(arguments) {
    let lastSector = Number(arguments.shift());
    let rowsIn1stSector = Number(arguments.shift());
    let numberOfSeatsOddRow = Number(arguments.shift());

    
}