function busFunction (arguments) {
    let passengers = Number(arguments.shift());
    let stations = Number(arguments.shift());

    for (let i = 1; i <= stations; i++) {
        let kachvashti = Number(arguments.shift());
        let slizashti = Number(arguments.shift());

        
    }
}