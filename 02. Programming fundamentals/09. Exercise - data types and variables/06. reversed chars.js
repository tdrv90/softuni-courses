function reversedChars(firstChar, secondChar, thirdChar) {
    let output = `${thirdChar} ${secondChar} ${firstChar}`;

    return output;
}

console.log(reversedChars('A', 'B', 'C'));
console.log(reversedChars('1', 'L', '&'));